// ${NAME}
// Created: ${DATE}

$fn = 50; // Circle resolution

// Example 3D model
difference() {
    // Main body
    cube([20, 20, 20], center = true);
    
    // Hole
    cylinder(h = 25, r = 5, center = true);
}
